/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // output: "export",
  images: {
    domains: ["res.cloudinary.com", "scontent.fplm4-1.fna.fbcdn.net", "admin.smkn1ba3.sch.id"]
  }
};

module.exports = nextConfig;
